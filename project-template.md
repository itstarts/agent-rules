# AGENTS.md — 项目级规范模板

> 通用工程纪律由全局 `AGENTS.md` 提供；本文件只记录可验证的项目事实和更具体的约束，不复制通用规则。
> 使用时复制到项目根目录并命名为 `AGENTS.md`，替换或删除全部 `<...>` 占位内容及不适用条目。
> 不记录密钥、令牌或个人隐私；只记录配置项名称、获取方式和适用环境。

## 项目事实

- 目标与非目标：<项目解决什么问题；本阶段明确不做什么>
- 技术栈与版本：<语言、框架、运行时、数据库和关键中间件的实际版本>
- 依赖与锁文件：<包管理器、安装命令和 lockfile 约定>
- 入口与主要模块：<应用入口、核心目录及职责>
- 文档与状态记录：<架构、接口、数据库、进度和交接文档的事实来源>
- 本地环境：<必要服务、端口、环境变量名称和启动前提>
- 分支与发布：<分支命名、目标分支、发布或部署入口>

## 路径与契约

| 类型 | 路径或事实来源 | 维护边界 |
| --- | --- | --- |
| 业务入口 | <路径> | <职责和调用边界> |
| 公共 API / DTO / 错误码 | <路径或文档> | <兼容性要求> |
| Schema / SQL / 迁移 | <路径和迁移工具> | <版本与执行顺序> |
| 公共组件或共享库 | <路径> | <主要消费者> |
| 配置与环境变量 | <路径或文档> | <允许修改的环境和默认值边界> |
| 生成文件 | <路径和生成命令> | 不手工修改；生成后检查无关 diff |
| 第三方或禁止修改区域 | <路径> | <原因和允许的例外> |

## 常用命令与验证

| 用途 | 工作目录与前提 | 命令 | 通过标准 |
| --- | --- | --- | --- |
| 初始化 / 安装 | <填写> | `<命令>` | <填写> |
| 本地运行 / 停止 | <填写> | `<命令>` | <端口、健康检查或退出标准> |
| lint / format / typecheck | <填写> | `<命令>` | <填写> |
| 单元测试 | <填写> | `<命令>` | <填写> |
| 集成测试 / 构建 | <填写> | `<命令>` | <填写> |

- 命令必须可直接执行；需要外部服务、凭证或特定环境时，明确前提但不记录敏感值。
- 在下方风险矩阵中按变更类型填写最小充分验证，避免无依据地只跑静态检查或机械运行全量测试。

## 项目高风险矩阵

只保留本项目实际存在的条目，并填写具体路径、契约或业务边界。

| 对象或路径 | 必须确认的边界 | 强制验证 | 回滚、前向修复或恢复方式 |
| --- | --- | --- | --- |
| <填写> | <兼容性、调用方、并发或数据影响> | <命令或场景测试> | <填写> |
| <填写> | <权限、安全、环境或失败影响> | <命令或场景测试> | <填写> |

- 至少按实际情况覆盖数据与迁移、公共契约、异步任务、资金与权限、配置与部署等高风险路径。
- 已应用或已发布的迁移不可删除、改写或重排；除非项目已有明确政策，否则通过新增迁移修正，并说明兼容性、锁、停机及恢复路径。
- 查询、索引、联表和模糊查询约束必须基于实际数据库、数据规模和执行计划，不设置缺少证据的统一默认值。
- MQ 或异步任务需记录实际投递语义、幂等或去重、重试、死信、并发和重入边界。

## 模板启用检查

- 已删除全部 `<...>` 占位内容和不适用条目。
- 所有路径、链接、命令和版本均已根据仓库现状验证。
- 项目规则没有重复或放宽全局安全、权限和最小充分验证要求。
- 模块存在更具体约束时，已在对应子目录增加更具体的 `AGENTS.md`。
- 文件中不包含密钥、令牌、凭证或个人隐私。
